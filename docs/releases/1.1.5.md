## Workstation reference subtitles

- Extract every matching supported English text-subtitle track as one transactional set instead of stopping on same-language candidates.
- Publish stable `.en.s<stream-index>` working copies and retain `.en` for the primary/default track.
- Upgrade Workstation manifests to v2 with ordered reference records, resolved stream indices, and an explicit primary track; v1 manifests remain readable.

## Transcription plans and recovery

- Compute quick/full/none status from the current preprocess plan, so historical transcription failures do not pollute a later policy.
- Resume incomplete preprocessing before an existing reference file can trigger human handoff.
- Give transcript/text outputs stable job, model, revision, and language identities; preserve completed jobs incrementally and support valid empty exports.

## Verification

- 45 automated tests passed.
- Python compilation and `git diff --check` passed.
- End-to-end `none` and `quick` runs passed on a 23-minute Matroska sample with two English SRT tracks; quick produced 462 segments and completed all six expected steps.
- Wheel and sdist metadata, package contents, and wheel installation were validated before publication.

## Upgrade note

Manifest v1 state is normalized compatibly when read and is persisted as v2 on the next state update. The package version participates in Stage tool fingerprints, so 1.1.5 may rerun stages whose tool identity changed; existing outputs remain protected by normal validation and transactional replacement.
